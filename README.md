# 杀毒软件查杀统计工具（Tk 版）

## 功能
- 支持文件夹或压缩包（zip/rar/7z）
- 支持密码解压（默认 `infected`，可明文修改）
- 支持选择解压根目录
- 前后快照差分统计（md5 / sha1 / sha256）
- 导出 CSV
- 生成 PNG 报告
- 报告右上角支持用户自备杀软图标
- 支持中英文界面与中英文报告模板

## 安装依赖
```bash
pip install -r requirements.txt
```

## 运行
```bash
python main.py
```
